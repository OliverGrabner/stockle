apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: stockle-ingress
  namespace: stockle
spec:
  ingressClassName: public
  rules:
    - host: api.stockle.fun
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: stockle-backend-service
                port:
                  number: 8080